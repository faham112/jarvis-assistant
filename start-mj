#!/usr/bin/env bash
set -Eeuo pipefail

APP_DIR="/var/www/html/jarvis-assistant"
PYTHON="${APP_DIR}/.venv/bin/python"

if [[ ! -x "$PYTHON" ]]; then
    printf 'Jarvis installation missing: %s\n' "$PYTHON" >&2
    exit 1
fi

cd "$APP_DIR"
exec "$PYTHON" "$APP_DIR/jarvis.py"
